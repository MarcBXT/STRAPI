{
  "routes": [
    {
      "method": "POST",
      "path": "/updateCatalog",
      "handler": "manage-catalog.import",
      "config": {
        "policies": [],
        "description": "Update catalogue with a file",
        "tag": {
          "plugin": "manage-catalog",
          "name": "manage",
          "actionType": "import"
        }
      }
    },
    {
      "method": "GET",
      "path": "/downloadCatalog",
      "handler": "manage-catalog.export",
      "config": {
        "policies": [],
        "description": "Dowload all data of the catalog in a file",
        "tag": {
          "plugin": "manage-catalog",
          "name": "manage",
          "actionType": "export"
        }
      }
    },
    {
      "method": "GET",
      "path": "/synchronize",
      "handler": "manage-catalog.synchronize",
      "config": {
        "policies": [],
        "description": "Retrieve analysis and receptacles updated since date",
        "tag": {
          "plugin": "manage-catalog",
          "name": "manage",
          "actionType": "synchronize"
        }
      }
    }
  ]
}
